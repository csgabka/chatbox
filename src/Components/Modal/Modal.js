import React from 'react';
import Notification from '../Notification/Notification';
import './Modal.css';

const modal = (props) => {
  return (
    <div className="Modal">
      <Notification />
    </div>
);
}

export default modal;
